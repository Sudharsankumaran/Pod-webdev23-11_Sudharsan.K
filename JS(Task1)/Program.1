1. Write a program to print the first 100 prime numbers. Create a function isPrime() that will take a number and return 1 or 0 if the number is prime or not.
 program:

function isPrime(n){
    var i,j,count;
    for(i=2;i<=n;i++){
        count=0;
        for(j=1;j<=i;j++){
            if(i%j==0){
                count=count+1;
            }
        }
        if(count==2){
            console.log(i);
        }
    }
}isPrime(100)

